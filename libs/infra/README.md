# persistence-memory

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build persistence-memory` to build the library.

## Running unit tests

Run `nx test persistence-memory` to execute the unit tests via [Jest](https://jestjs.io).
